-----

# 🛡️ Titanium-T (TrojanStallion Core)

> **下一代 Cloudflare Worker 边缘代理方案 | 基于 Trojan 协议 + ReactionMax 高可用架构**

[](https://workers.cloudflare.com/)
[](https://trojan-gfw.github.io/trojan/)
[](https://www.google.com/search?q=./LICENSE)

**Titanium-T** 是一个部署在 Cloudflare Workers 上的轻量级、高隐蔽性代理服务。它抛弃了复杂的 VLESS UUID 机制，转而使用更符合 HTTPS 流量特征的 **Trojan 协议**，并集成了独家的 **ReactionMax** 连接保活引擎，提供极致的稳定性和生产级体验。

-----

## 🆚 核心优势：为什么选择 Trojan 版本？

相较于常见的 VLESS+WS 方案，Titanium-T 采用的 Trojan 协议具有以下显著优势：

| 特性 | 🟢 Titanium-T (Trojan) | 🟡 传统 VLESS Worker | 核心差异解析 |
| :--- | :--- | :--- | :--- |
| **鉴权方式** | **简单密码** (如 `abc`) | 复杂 UUID | Trojan 密码更易记忆，且在 TLS 握手后传输，极难被探测。 |
| **流量特征** | **HTTPS 拟真度极高** | 通用加密流 | Trojan 设计初衷即为“伪装成 HTTPS 服务”，在严苛的网络环境中存活率更高。 |
| **配置复杂度** | **低** | 中 | 无需生成和校验 UUID，配置更简洁直观。 |
| **头部开销** | **极小** | 较小 | Trojan 协议头部非常精简，理论上处理速度微幅优于 VLESS。 |
| **客户端兼容** | **极佳** | 优秀 | 几乎所有现代代理客户端（Clash, Shadowrocket, v2rayN）都完美支持 Trojan。 |

-----

## ✨ 独家特性

### 1\. ⚡ ReactionMax 高可用引擎

这不是一个简单的转发脚本。内置的 ReactionMax 引擎包含：

  * **多策略自动重连**：连接失败时自动尝试直连、SOCKS5 代理或备用反代 IP。
  * **智能心跳 (Smart Heartbeat)**：主动检测连接停滞，防止长连接被中间设备切断。
  * **动态评分系统**：实时监测吞吐量，自动调整重连策略。

### 2\. 🎭 变色龙伪装 (Chameleon Mode)

  * **默认行为**：直接访问 Worker 域名，显示一个精美的 **"技术笔记" 中文博客**，完美通过人工审查。
  * **管理行为**：只有访问特定格式的隐藏路径（如 `/config_ab.html`）时，才会显示管理面板，避免直接暴露密钥。

### 3\. 🎛️ 智能配置面板

无需手动拼接链接。访问管理面板，即可：

  * 自动识别当前 Worker 域名。
  * 一键填入优选 IP。
  * 配置 SOCKS5 前置代理（链式代理）。
  * **自动生成 Trojan 链接和 Clash YAML 配置**。

-----

## 🛠️ 部署教程

### 第一步：获取代码

1.  下载或复制本项目中的 `worker.js` 代码。

### 第二步：部署到 Cloudflare

1.  登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)。
2.  进入 **Workers & Pages** -\> **Create Application** -\> **Create Worker**。
3.  命名你的 Worker（例如 `titan-trojan`），点击 **Deploy**。
4.  点击 **Edit code**，将默认代码清空，粘贴 `worker.js` 的全部内容。

### 第三步：修改核心配置

在代码顶部找到 `全局配置` 区域，修改以下配置：

```javascript
// worker.js 第 11 行左右
const 全局配置 = {
    // 【重要】这是你的 Trojan 密码
    密钥: "your_password", 
    
    // 配置面板访问路径前缀（访问方式：/config_密钥前2位.html）
    配置面板路径: "config",
    
    // 其他配置可保持默认...
};
```

**配置说明：**
- `密钥`：你的 Trojan 密码，建议设置复杂密码（如 `MySecretPass2025`）
- `配置面板路径`：管理面板的 URL 前缀，可自定义（如改为 `panel`、`admin` 等）

### 第四步：保存并部署

点击编辑器右上角的 **Deploy** 保存更改。

### 第五步. (关键优化) 防止偷跑请求数

Cloudflare Worker 免费版每天有 10 万次请求限制。互联网上存在大量扫描器，即使无法破解您的密码，扫描默认的 `*.workers.dev` 域名也会消耗您的请求配额。

**建议执行以下操作：**

1.  在 Worker 的 **Settings** -\> **Triggers** -\> **Custom Domains** 中，绑定您的自定义二级域名（例如 `vless.yourdomain.com`）。
2.  在下方的 **Routes** 区域，找到默认的 `xxx.workers.dev` 路由。
3.  点击 **Disable Route** (禁用路由)。
    > **注意**：这样做之后，您只能通过自定义域名访问，彻底杜绝针对默认域名的扫描攻击。

-----

## ⚙️ 使用指南

### 1\. 访问管理面板

部署完成后，在浏览器访问你的 Worker URL，使用以下格式：

> `https://你的域名.workers.dev/{配置面板路径}_{密钥前2位}.html`

**示例：**
- 如果密钥是 `MySecretPass2025`，配置面板路径是 `config`
- 则访问：`https://titan-trojan.username.workers.dev/config_My.html`

**安全性说明：** 这种方式避免在 URL 中直接暴露完整密钥，看起来像普通 HTML 文件，提高隐蔽性。

### 2\. 生成节点配置

在面板中：

  * **地址 (Address)**: 填入你的优选 IP 或优选域名（默认为 `www.shopify.com`）。
  * **点击生成**: 获取 Trojan 链接或 Clash 配置。
  * **导入客户端**: 将生成的链接导入 v2rayN、Clash Verge、Shadowrocket 等软件即可使用。

### 3\. 高级玩法：SOCKS5 链式代理

如果你有一台解锁了 ChatGPT/Netflix 的 SOCKS5 代理（例如 `1.2.3.4:1080`），你可以在面板中填入：

  * Worker 自动生成带有 `/s5=...` 参数的路径。
  * 流量路径：`你 -> Worker (Trojan) -> SOCKS5 -> 目标网站`。
  * **效果**：隐藏 Worker IP，实现流媒体解锁。

-----

## 📱 客户端配置示例

### Clash / Clash Meta (YAML)

```yaml
proxies:
  - name: Titan-Trojan
    type: trojan
    server: 你的优选IP或域名
    port: 443
    password: 你的密码
    udp: true
    sni: 你的Worker域名.workers.dev
    skip-cert-verify: true
    network: ws
    ws-opts:
      path: "/my-key=你的密码/"
      headers:
        Host: 你的Worker域名.workers.dev
```

### v2rayN / Nekobox (URL)

```text
trojan://你的密码@优选IP:443?security=tls&sni=Worker域名&type=ws&host=Worker域名&path=%2Fmy-key%3D你的密码%2F#Titan-Trojan
```

-----

## ⚠️ 免责声明

1.  本项目仅供网络技术研究、学习 Cloudflare Workers 机制及通信协议原理使用。
2.  严禁将本项目用于任何违反当地法律法规的用途。
3.  作者不对使用本项目产生的任何后果负责。

-----

**Star ⭐ 这个项目，如果它对你有帮助！**

**Made with ❤️ by Google-Gemini**
